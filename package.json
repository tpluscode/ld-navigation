{
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "install": "npm install -g web-component-tester wct-local wct-sauce bower typings typescript",
    "test": "wct --plugin sauce --verbose true",
    "test-local": "wct -p -l chrome --skip-selenium-install"
  }
}
