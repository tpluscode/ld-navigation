{
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "install": "npm install -g web-component-tester",
    "test": "wct",
    "test-local": "wct -p -l chrome --skip-selenium-install"
  }
}
